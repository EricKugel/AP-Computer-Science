/**
 * A customer has a name and ID number.
 */
public class Customer {
  private String name = null;
  private int idNum = 0;
  
  /** 
   * Constructs a customer with given name and ID number.
   * Not implementd in the original AP question.
   * 
   * @param name   the name of this customer
   * @param idNum  the ID number of this customer
   */
  public Customer(String name, int idNum) {
    this.name = name;
    this.idNum = idNum;
  }
  
  /**
   * Returns the name of this customer.
   * Not implementd in the original AP question.
   * 
   * @return  the name of this customer
   */
  public String getName() {
    return name;
  }
  
  /**
   * Returns the ID number of this customer.
   * Not implementd in the original AP question.
   * 
   * @return  the ID number of this customer
   */
  public int getID() {  
    return idNum;
  }
  
  /**
   * Returns 0 when this customer is equal to other;
   *         a positive integer when this customer is greater than other;
   *         a negative integer when this customer is less than other.
   *
   * @return  a value to show how this customer compares to other
   */
  public int compareCustomer(Customer other) {
    return name.compareTo(other.getName()) != 0 ? name.compareTo(other.getName()) > 0 ? 1 : -1 : getID() > other.getID() ? 1 : -1;
  }
  
  
  /**
   * Fills result with customers merged from the beginning of array1 and array2; 
   * result contains no duplicates and is sorted in ascending order by customer.
   * Precondition:  result.length > 0;
   *                array1.length >= result.length;
   *                array1 contains no duplicates;
   *                array2.length >= result.length;
   *                array2 contains no duplicates;
   *                array1 and array2 are sorted in ascending order by customer
   * Postcondition: array1, array2 are not modified
   * 
   * @param array1  the first set of customers to be merged
   * @param array2  the second set of customers to be merged
   * @param result  the merged set comprised of array1 and array2
   */
  public static void prefixMerge(Customer[] array1, Customer[] array2, Customer[] result) {
    int i1 = 0;
    int i2 = 0;
    while (i1 < )
  }
  
  
  /**
   * Returns the name and ID number of this customer.
   * Not included in the original AP question.
   * 
   * @return  a string representation of the object
   */
  public String toString() {
    return name + ": " + idNum;
  }
  
}